

## Plan : Améliorer la visibilité du TrustedBy

### Changements dans `src/components/TrustedBy.tsx` :

1. **Texte plus grand** : Passer le texte "Ils m'ont fait confiance" de `text-xs` à `text-sm font-medium`

2. **Effet hover plus prononcé** : Changer `group-hover:scale-110` en `group-hover:scale-125` pour un grossissement plus visible au survol

